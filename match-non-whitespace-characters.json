{"index.js":"let sample = \"Whitespace is important in separating words\";\nlet countNonWhiteSpace = /\\S/g; // search all characters that aren't white space, globally\nlet result = sample.match(countNonWhiteSpace);"}